const counter = (arr) => {
    return 'There are ' + arr.length + ' elements in the arr'
}

const adder = (a,b) => {
    return `the sum of two numbers is ${a+b}`
}

const pi=3.14
module.exports = {
    counter,
    adder,
    pi
}